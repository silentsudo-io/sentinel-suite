---
layout: sentinel-ref
title: "test_engine_semantics.py"
blurb: "Azimuth (Python) · unversioned · 357 lines"
---

# test_engine_semantics.py

> `Sentinel/Azimuth/engine/tests/test_engine_semantics.py`

| | |
|---|---|
| **Family** | Azimuth (Python) |
| **Version** | — |
| **Size** | 357 lines |
| **Scope** | **public** — ships in `sentinel-suite` |
| **Documented by** | [SENTINEL_AZIMUTH_SPEC](../../SENTINEL_AZIMUTH_SPEC.md) |

> Rendered from the **published** copy in `sentinel-suite/src/`, not the author's private tree — so this page describes the file you actually have.

## What the file says about itself

```text
Engine semantics -- one position at a time, the conflict rule, the declared
SL/TP resolution order, warmup, continuous mode and the force-flat cascade.
```

